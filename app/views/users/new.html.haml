%h1 Register
= form_with model: @user, url: register_path, local: true do |f|
  - if @user.errors.any?
    %ul
      - @user.errors.full_messages.each do |msg|
        %li= msg
  %div
    = f.label :name
    = f.text_field :name, class: "text-black"
  %div
    = f.label :password
    = f.password_field :password, class: "text-black"
  %div
    = f.submit "Register"